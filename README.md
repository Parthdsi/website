# Parth's Website

A personal portfolio built with React + Vite.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

Output goes to `dist/`. For GitHub Pages, deploy the contents of `dist/` to your gh-pages branch or configure your workflow to build and push.
